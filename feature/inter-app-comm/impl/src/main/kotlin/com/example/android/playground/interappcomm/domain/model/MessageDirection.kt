package com.example.android.playground.interappcomm.domain.model

/** Whether this message was produced by THIS app (SENT) or arrived from the OTHER app (RECEIVED). */
enum class MessageDirection { SENT, RECEIVED }
